# Чат «Иван Петров» — спецификация для разработки, iOS

Документ описывает поведение и размеры так, чтобы экран можно было
собрать, не заглядывая в мокап. Все числа сняты замерами с работающего
прототипа, а не переписаны из макета.

Базовый экран **393 × 852** (iPhone 15/16, логические точки). Координаты
в тексте — от левого верхнего угла экрана.

---

# 1. Основа

## 1.1 Палитра

| Токен | Значение | Где |
|---|---|---|
| `accent` | `#0BA686` | акценты, иконка файла |
| `accentDark` | `#0A8E73` | статус «В сети», подпись типа файла |
| `accentDeep` | `#09856B` | правый край градиента пузыря |
| `ink` | `#15110F` | основной текст |
| `inkSecondary` | `#15110F` 64% | метки времени, вес файла |
| `inkTertiary` | `#15110F` 24% | плейсхолдер поля ввода |
| `surface` | `#F0F0F0` | фон чата |
| `surfaceCard` | `#F5F5F5` | фон ленты документа |
| `paper` | `#FFFFFF` | входящий пузырь, лист документа |
| `backdropDark` | `#141413` | фон просмотрщика |
| `destructive` | `#FF3B30` | зарезервирован, сейчас не используется |
| `overlayMedium` | чёрный 32% | плашка даты, метка на медиа |

Заливка исходящего пузыря — линейный градиент слева направо:
`#0BA686 → #09856B`, вторая точка на 98.5%.

## 1.2 Скругления

| Элемент | Радиус |
|---|---|
| Корпус устройства | 54 |
| Шторка сверху | 38 |
| Пузырь сообщения | 16 |
| Медиа в пузыре | 15 |
| Плашка файла | 12 |
| Пилюли и круглые кнопки | полная |
| Контекстное меню | 32, пункт 22 |
| Мини-экран | 14 |
| Миниатюра в ленте | 5 |

## 1.3 Типографика

Системный шрифт. **Трекинг вручную не задавать** — в вебе он выставлялся
(−0.4 для тела), потому что там нет оптического кернинга по размеру.
В UIKit и SwiftUI системный шрифт несёт правильный трекинг сам, и ручная
поправка расшатает текст относительно системных контролов.

| Роль | Размер / интерлиньяж | Начертание |
|---|---|---|
| Тело сообщения | 17 / 22 | regular |
| Имя собеседника, имя файла в шапке | 16 / 20 | semibold |
| Имя файла в плашке | 15 / 20 | regular |
| Статус, вес файла, счётчики | 13 / 18 | regular |
| Метка времени | 11 / 13 | regular |
| Пункт меню | 18 / 22 | regular |

## 1.4 Стекло

Полупрозрачные поверхности — системный материал. В мокапе это собиралось
вручную (размытие + градиент + два слоя обвода), потому что в браузере
системного материала нет.

- Светлый тон: материал + заливка белым `0.80 → 0.60` под 135°.
- Тёмный тон: материал + `rgba(68,68,68,.13) → rgba(73,73,73,.75)` под 135°.
- Обвод 0.5, вертикальный градиент: светлый `1 → .16 → .8`,
  тёмный `.8 → .08 → 1`.

**На iOS 26** это место заменяется системным жидким стеклом — оно даёт
живое преломление, которого в вебе не было ничем.

## 1.5 Тайминги

| Событие | Длительность | Кривая |
|---|---|---|
| Возврат обвязки просмотрщика | 0.34 с | `(0.32, 0.72, 0, 1)` |
| Уход обвязки | 0.30 с | `(0.5, 0, 0.75, 0)` |
| Прозрачность обвязки | 0.26 / 0.24 с | ease, при возврате задержка 0.06 |
| Раскрытие меню | 0.34 с | `(0.25, 0.9, 0.3, 1)`, без отскока |
| Закрытие меню | 0.17 с | `(0.4, 0, 1, 1)` |
| Раскрытие миниатюры | 0.28 с | `(0.2, 0.7, 0.3, 1)` |
| Примагничивание мини-экрана | 0.34 с | пружина, демпфирование 0.78 |
| Появление и уход счётчиков | 0.25 с | ease |

Задержки:

| Что | Сколько |
|---|---|
| Автоскрытие обвязки после старта видео | 1 с |
| То же после возвращающего тапа | 3 с |
| Жизнь счётчика кадров | 2 с |
| Жизнь счётчика страниц | 1 с |
| Автоскрытие кнопок мини-экрана | 2 с |

---

# 2. Экран чата

## 2.1 Раскладка

| Блок | Координаты |
|---|---|
| Верхняя панель | 0..116 (статус-бар 62 + шапка 44 с полями) |
| Нижняя панель | 736..852 |
| Ряд «Саммари» | 736..776 |
| Поле ввода | 780..852 |
| Отступ ленты сверху | 120 |
| Отступ ленты снизу | 136 |

Отступ снизу подобран так, чтобы между последним сообщением и кнопкой
«Саммари» был тот же зазор 8, что между сообщениями.

## 2.2 Сообщения

Вертикальный отступ ряда 4 сверху и снизу — между соседними пузырями
получается 8.

| Тип ряда | Поля ряда |
|---|---|
| Входящий текст | слева 10, справа 42 |
| Исходящий текст | слева 87, справа 10 |
| Входящее медиа | слева 10, справа 42 |
| Исходящее медиа | слева 42, справа 10 |

Текст в пузыре: поля 9 по горизонтали, 6 по вертикали.

**Метка времени** лежит абсолютно в правом нижнем углу пузыря, поля
2.5 / 7.5. Чтобы она не наезжала на последнюю строку, в конце текста
резервируется место — распорка шириной 42 (у исходящих 58, там ещё
галочка). Это не декоративный приём: без него текст и время
перекрываются на любой строке, доходящей до правого края.

**Хвостик** — отдельная фигура 12×15, смещение 5 наружу, прижата к низу.
Не часть скругления: пузырь и хвост красятся по-разному, у исходящего
хвост берёт крайний цвет градиента.

## 2.3 Растворение ленты под панелями

Сообщения уходят под обе панели с нарастающим размытием — не обрезаются
линией и не гаснут прозрачностью.

Пять слоёв, радиусы **1, 2, 4, 9, 18**, у каждого своя маска-градиент:
слабый работает почти на всей высоте зоны, сильный — только у кромки.
Поверх цветовая вуаль к `surface`.

Высота зоны: сверху 136, снизу 140.

Одним слоем это не собрать: получается равномерная муть с видимой
ступенькой на границе. Публичного variable blur в системе нет — если
появится, конструкция заменяется одним вызовом.

## 2.4 Плашка файла

Высота 56, поля 8, скругление 12, фон `accent` 12%.

Слева иконка 40×40: лист, загнутый угол 13×13 в правом верхнем углу и
подпись типа по центру, 13 цветом `accentDark`. Справа кнопка скачивания
40×40.

Между плашками 4. Снизу у пузыря с файлами отступ 24 — под метку времени.

---

# 3. Просмотрщик документа

Открывается тапом по плашке. Экран чата уезжает назад
(сдвиг 50 вниз, масштаб 0.918) и притемняется на 10%.

## 3.1 Раскладка

| Блок | Координаты |
|---|---|
| Шторка | 60..852, скругление 38 сверху |
| Шапка | 78..122, поля 16 |
| Первая страница | 130 |
| Лист A4 | 393 × 546, поля 35.6 / 36.5 |
| Зазор между листами | 8 |
| Остров действий | 147..247, y 780..824 |

Отступы ленты: 70 сверху, 84 снизу.

**Полоски-хвата сверху нет.** Шторка тянется за верхние 70 точек — зона
перекрывает шапку.

## 3.2 Анимация появления

Шторка въезжает снизу изменением **вертикальной позиции**, а не
трансформацией.

В вебе это было вынужденно: трансформация на предке отключает размытие
подложки у вложенных элементов, и всё стекло внутри шторки становилось
плоским. В нативе ограничения нет, но поведение стоит сохранить — оно
влияет на то, как ведут себя тени и материалы при движении.

## 3.3 Типы документов

Набор страниц подставляется по расширению.

| Тип | Что показывается | Страниц |
|---|---|---|
| docx | A4, текст | 3 |
| pdf | обложка, палитра, типографика, знак, применение | 5 |
| pptx | слайды 16:9, 361×203, скругление 8, поля 16 | 4 |
| xlsx | лист с буквами колонок и вкладками снизу | 2 |
| html | рендер страницы + исходник с подсветкой | 2 |
| md | отрендеренный markdown | 1 |
| txt | моноширинный текст | 1 |

В продакшене сюда встают PDFKit, QuickLook или веб-вью. Разметка нужна,
чтобы видеть раскладку и отступы.

## 3.4 Счётчик страниц

Формат «N из M». Живёт 1 секунду после остановки прокрутки, не
показывается при одной странице.

**Номер считается по фактическим позициям листов**, а не делением
прокрутки на фиксированный шаг: у слайдов, таблиц и A4 разная высота.

Счётчик и шапка не сосуществуют: как только шапка начинает возвращаться
(схлопывание меньше 98%), счётчик убирается сразу, не дожидаясь таймера.

## 3.5 Схлопывание обвязки

На первых **100 точках** прокрутки шапка и остров действий уменьшаются до
нуля и гаснут. Пилюля с именем файла дополнительно ужимается до 70% и
поднимается на 8.

## 3.6 Действия

Внизу один остров 100×44 по центру: переслать и поделиться.

В контекстном меню: показать в чате, ответить, печать. Сворачивания у
документов нет.

---

# 4. Просмотрщик медиа

Открывается тапом по кадру. Фон `backdropDark`.

## 4.1 Раскладка

| Блок | Координаты |
|---|---|
| Шапка | 62..106 |
| Счётчик кадров | 114..140, по центру |
| Плеер | 28..365, y 670..714 |
| Лента миниатюр | 722..770 |
| Панель действий | 770..852 |

## 4.2 Вписывание кадра

Размер считается арифметикой:

```
w = min(доступная ширина, доступная высота × соотношение)
h = w / соотношение
```

**Не через соотношение сторон с ограничением по высоте.** Такая связка
режет высоту, оставляет ширину, и длинный вертикальный снимок молча
обрезается. Проблема воспроизводится и в вебе, и в SwiftUI.

Пропорции хранятся у каждого кадра. Длительность видео — тоже: она
управляет и плеером, и наличием перемотки.

## 4.3 Лента миниатюр

Механика как в Telegram.

- Высота 36, скругление 5, зазор 4.
- Невыбранные кадры узкие: **20**.
- Выбранный раскрывается по своим пропорциям: квадрат до 36, панорама
  640:274 до 84.
- Вся лента умещается в кадр с полями **24**, прокрутки нет.
- Если широкий выбранный не влезает, невыбранные ужимаются
  пропорционально, нижний предел 10.
- Неактивные накрыты чёрным 40%, активный без накладки.

## 4.4 Подпись

Подпись сообщения показывается целиком и разворачивается прокруткой.

| Параметр | Значение |
|---|---|
| Слой прокрутки | 62..852 |
| Верх текста в покое | 644 |
| Хвост под текстом | 210 (видео), 158 (фото) |
| Затемнение кадра при развороте | до 65% |

В развёрнутом состоянии последняя строка встаёт в **28** над нижним
контентом — над плеером, если он есть, иначе над лентой.

Снизу текст растворяется маской; сверху обрезается по границе
статус-бара без растушёвки.

### Правила взаимодействия

Три вещи, без которых подпись ломается:

1. **Слой прозрачен для нажатий, ловит только сам текст.** Иначе
   распорка накрывает кнопку воспроизведения, и по видео нельзя попасть.
2. **Касание подписи останавливает автоскрытие обвязки.** Без этого на
   видео таймер срабатывает посреди жеста и текст не поднять.
3. **Поднятая подпись идёт поверх кнопки воспроизведения**, но под
   шапкой.

Порядок слоёв снизу вверх: кадр → вуали → кнопки воспроизведения →
подпись → полоса размытия → шапка, плеер, лента.

Боковые кнопки шапки уходят под текст, когда он до них доехал. Порог
считается по реальным позициям — сравнением верхней кромки текста с
нижней кромкой кнопок, а не по доле прокрутки.

## 4.5 Полоса размытия внизу

200 точек, радиусы **1, 2.5, 5, 10**. Лежит выше текста, но ниже плеера
и ленты: расплываются кадр и наехавшие на него строки, обвязка остаётся
резкой.

## 4.6 Плеер

Пилюля 44 высотой с полями 28. Внутри: время слева, дорожка, хронометраж
справа. Полоса 6, скругление 6.

Ролик идёт по кругу. По дорожке можно ткнуть в нужную точку. Перемотки
±15 нет — она была и убрана.

## 4.7 Режим просмотра

Через **1 секунду** после запуска обвязка уезжает: шапка вверх на 120,
футер вниз на свою высоту, синхронно. Плеер превращается в полосу 6 и
опускается на место кнопок (низ 50), дорожка исчезает, остаётся
индикатор цветом `#777675`.

Тап по кадру возвращает обвязку; если воспроизведение продолжается, она
прячется снова через 3 секунды.

**Тап по фото работает переключателем**: убирает обвязку и возвращает —
чтобы смотреть снимок без лишнего.

Режим **не включается**, пока открыто контекстное меню или поднята
подпись.

## 4.8 Кадр при воспроизведении неподвижен

Наезд и колыхание были и убраны. Причины стоит знать, чтобы не вернуть
их случайно:

- Искажение через карту смещений роняло частоту кадров с 43 до 15, и все
  переходы начинали дёргаться.
- Наезд с плавающим масштабом заставлял заново растрировать слой каждый
  кадр — та же просадка.

Если движение всё же нужно, безопасный вариант — постоянный масштаб и
анимация только смещения.

## 4.9 Действия

Внизу три кнопки 44×44 с полями 28: переслать, «поделиться» (у видео
вместо неё скорость), сохранить.

В меню: показать в чате, ответить, свернуть, удалить. Все пункты одного
цвета, красного выделения нет.

---

# 5. Контекстное меню

## 5.1 Внешний вид

| Параметр | Значение |
|---|---|
| Ширина | 238 (обычное), 200 (скорость) |
| Скругление | 32, поля 10 |
| Пункт | 40 высотой, скругление 22, поля 10 |
| Текст | 18 / 22 |
| Иконка | клетка 24 слева, зазор 8 |
| Блюр | 5.33 |

Контурные иконки ужимаются до **20** (печать до **17**): они нарисованы
во всю клетку, а иконки из экспорта имеют внутренние поля, и при равной
рамке вес не совпадает.

**Экран под меню не затемняется.** Подложка прозрачная и нужна только
чтобы поймать нажатие мимо.

## 5.2 Наборы пунктов

| Набор | Пункты | Высота |
|---|---|---|
| Документ | показать в чате, ответить, печать | 140 |
| Медиа | показать в чате, ответить, свернуть, удалить | 180 |
| Скорость | 0.5x, обычная, 1.5x, 2x | 180 |

«Свернуть» доступно у видео. На фото пункт выключен и погашен до 40%.

## 5.3 Анимация

Меню появляется каплей: кружок 34×34 со скруглением 50% раздувается до
полного размера, скругление едет к 32. Длительность 0.34, без отскока.

Содержимое проявляется отдельной анимацией с задержкой в треть цикла —
иначе текст растягивается вместе с каплей.

Меню крепится **за правый край**, а не за левый: капля должна расти от
угла кнопки внутрь экрана. Меню скорости центрируется по оси кнопки
сдвигом на половину собственной ширины — так центр держится на месте всю
анимацию.

В нативе то же достигается масштабированием с якорем в углу: дешевле по
отрисовке и не требует пересчёта раскладки каждый кадр.

---

# 6. Мини-экран

Сворачивание по пункту «Свернуть» в меню медиа. Воспроизведение не
прерывается, чат под окном виден и работает.

## 6.1 Размеры

| Параметр | Значение |
|---|---|
| Ширина базовая | 150 |
| Горизонтальные кадры | 150 × соотношение |
| Потолок ширины | ширина экрана минус 20 |
| Скругление | 14 |
| Кнопки закрыть и развернуть | 28 |
| Воспроизведение | 46, перемотка 32 |
| Полоса прогресса | по нижней кромке, поля 8, высота 4 |

## 6.2 Перетаскивание

Окно **догоняет палец с оттяжкой**: положение сходится к цели по 22% за
кадр — получается запаздывание и инерция при резком движении.

На отпускании примагничивается к ближайшему из четырёх углов. Ближайший
определяется по тому, в какой половине экрана оказался центр окна по
каждой оси.

Границы: **10** по горизонтали, **56** сверху, **40** снизу.

Тап и перетаскивание разведены порогом **3 точки**: короткое касание
разворачивает, движение двигает. Без порога окно разворачивалось после
каждого переноса.

## 6.3 Обвязка мини-экрана

Кнопки, блок воспроизведения и полоса прогресса гаснут через **2 секунды**
бездействия. Тап возвращает.

При скрытых кнопках первый тап только возвращает их, не разворачивая
окно. Состояние нужно запоминать **в момент касания**: обработчик
нажатия успевает показать кнопки раньше, чем сработает тап, и проверка
увидит уже показанное состояние.

Плеер и полоса показываются только у видео. Перемотка ±10 — только если
ролик длиннее самого шага.

---

# 7. Состояния и переходы

```
        ┌──────────┐  тап по файлу   ┌────────────────┐
        │   Чат    │────────────────►│    Документ    │
        │          │◄────────────────│                │
        └────┬─────┘  закрыть, свайп └───────┬────────┘
             │                               │ «ещё»
             │ тап по кадру                  ▼
             ▼                        ┌────────────┐
        ┌──────────┐    «ещё»         │    Меню    │
        │  Медиа   │─────────────────►│            │
        │          │◄─────────────────│            │
        └────┬─────┘                  └────────────┘
             │ «свернуть»
             ▼
        ┌──────────┐  развернуть
        │  Мини-   │──────────────► Медиа
        │  экран   │  закрыть ────► Чат
        └──────────┘
```

Обвязка просмотрщика — отдельная ось состояния, независимая от экрана:

```
видимая ──(1 с после старта видео)──► скрытая
        ──(тап по фото)─────────────► скрытая
скрытая ──(тап по кадру)────────────► видимая
        ──(открытие меню)───────────► видимая
        ──(касание подписи)─────────► видимая
```

Переход в скрытую **блокируется**, если открыто меню или подпись поднята.

---

# 8. Что стоит сделать иначе, чем в мокапе

Ограничения браузера, которых в нативе нет:

- **Иконки.** В мокапе все контуры нарисованы путями, потому что система
  не отдаёт приватную область шрифта по имени семейства. В приложении
  берите SF Symbols по имени.
- **Смена play/pause** собиралась вручную из двух слоёв с поворотом. В
  нативе есть штатный переход между символами.
- **Стекло** описано в 1.4 — на новых системах заменяется системным
  материалом целиком.
- **Мини-экран** здесь свой, внутри приложения. Системный PiP выносит
  окно за пределы приложения, рисуется самой ОС, и поведение будет
  другим. Решение продуктовое: своё окно управляемо, системное привычнее.
- **Воспроизведение** в мокапе имитировано таймером на 42 секунды.
  Интерфейс сведён к трём вещам — прогресс, флаг воспроизведения,
  переключение, — чтобы на это место без переделок встал плеер.

# 9. Чего в мокапе нет

Чтобы не искать: зума по двойному тапу, свайпа между кадрами внутри
просмотрщика, сворачивания подписи до «Ещё» в ленте, реальной загрузки
файлов, состояний ошибки и пустых экранов.
